import React from 'react';

const Button = () => {
    return (
        <div>
         <button class="btn border-0 btn-active bg-gradient-to-r from-primary to-secondary ">Button</button>
        </div>
    );
};

export default Button;